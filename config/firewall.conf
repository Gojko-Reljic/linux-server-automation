# firewall.conf
# -------------------------------------------------------------------
# One port (or port/protocol) per line to allow through the firewall.
# -------------------------------------------------------------------
22/tcp
80/tcp
443/tcp
